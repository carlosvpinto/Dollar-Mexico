<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Night Mode. -->
    <style name="Theme.Dollar_Mexico.Night" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color in Night Mode -->
        <item name="colorPrimary">@color/gray_800</item>
        <item name="colorPrimaryVariant">@color/gray_900</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color in Night Mode -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background color for Night Mode -->
        <item name="android:background">@color/black</item>

        <!-- Status bar color in Night Mode -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Text color for better contrast in Night Mode -->
        <item name="android:textColor">@color/white</item>

        <!-- Button normal background color -->
        <item name="android:colorButtonNormal">@color/teal_700</item>

        <!-- Customize your theme here. -->
    </style>
</resources>
